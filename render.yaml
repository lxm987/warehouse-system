services:
  - type: web
    name: warehouse-system
    env: node
    buildCommand: cd server && npm install
    startCommand: node server/index.js
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: "10000"
